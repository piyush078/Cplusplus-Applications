/**
 * GAME DISPLAY VARIABLES.
 * Changing them will change the orientation of the game grid.
 */

/**
 * USER-DEFINED VARIABLES
 * Changing them depends on user's interest.
 *
 * Boundaries limits of the game area
 * Speed of the snake from 1 (slowest) to 10 (fastest)
 */
#define _TOP_LIMIT 0
#define _BOTTOM_LIMIT 10
#define _LEFT_LIMIT 0
#define _RIGHT_LIMIT 40
#define _SPEED 7
